spring:
  profiles:
    active: dev
  application:
    name: user-center
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        enable: true
        file-extension: yaml
        prefix: user